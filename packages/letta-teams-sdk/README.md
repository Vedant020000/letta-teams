# letta-teams-sdk

Reusable SDK for orchestrating teams of stateful Letta agents.

This package contains the runtime, storage layer, daemon/client bridges, and core orchestration APIs extracted from `letta-teams`.
